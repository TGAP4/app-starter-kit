type Mutation {
  postUser(firstName: String!, lastName: String!): User!
}

type Query {
  getUser(id: Int!): User
}

type User {
  firstName: String!
  fullName: String!
  id: ID!
  lastName: String!
}